import * as React from 'react';

export interface book_type {
    id: '';
    volumeInfo: {
      description: '';
      imageLinks: {
        thumbnail: '';
        smallThumbnail: '';
      };
      infoLink: '';
      title: '';
    };
  }